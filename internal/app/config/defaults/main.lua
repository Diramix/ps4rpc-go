return {
    core = {
        ip = "",
        autostart = true,
    },
    rpc = require("rpc"),
    bot = require("bot"),
    cache = require("cache"),
    dev = require("dev"),
    mapped = require("mapped"),
}
