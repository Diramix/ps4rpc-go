return {
    core = {
        ip = "",
        autostart = true,
    },
    activity = require("activity"),
    bot = require("bot"),
    cache = require("cache"),
    dev = require("dev"),
    mapped = require("mapped"),
}
